'use strict';

var mixin = require('deep-extend');

var defaults = {
  host: '127.0.0.1',
  port: '16379'
};

module.exports = {
  development: defaults,
  test: defaults,
  production: mixin({}, defaults, {
    port: '6379'
  })
};

