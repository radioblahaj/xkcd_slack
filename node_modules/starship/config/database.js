'use strict';

var mixin = require('deep-extend');

var defaults = {
  database  : 'starship_dev',
  username  : 'walle',
  password  : 'eve',

  dialect   : 'postgres',
  protocol  : 'postgres',
  host      : '127.0.0.1',
  port      : 15432,
  native    : true,
  query     : {
    pool    : true,
    debug   : true
  },
  define: {
    timestamps  : true,
    underscored : true,
    charset     : 'utf8'
  },
  logging : console.log
};

module.exports = {
  development: defaults,
  test: mixin({}, defaults, {
    database: 'starship_test'
  }),
  production: mixin({}, defaults, {
    database: 'starship_prod'
  })
};
